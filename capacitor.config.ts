import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stpl.sobliftingtracker',
  appName: 'STPL SOB TRACKER',
  webDir: 'dist'
};

export default config;
